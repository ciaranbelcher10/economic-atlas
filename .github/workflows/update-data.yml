name: Refresh data
on:
  schedule:
    # Hourly, every day.
    - cron: "0 * * * *"
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests openpyxl xlrd
      - run: python fetch_data.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_us.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_ez.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_jp.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          ESTAT_APP_ID: ${{ secrets.ESTAT_APP_ID }}
      - run: python fetch_in.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          MOSPI_USERNAME: ${{ secrets.MOSPI_USERNAME }}
          MOSPI_PASSWORD: ${{ secrets.MOSPI_PASSWORD }}
      - run: python fetch_ca.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_au.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_kr.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_il.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_mx.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_br.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_za.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_ma.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_de.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_fr.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_it.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_es.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_nl.py
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
      - run: python fetch_uk_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_us_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_jp_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_in_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_ca_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_au_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_kr_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_il_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_mx_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_br_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_za_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_ma_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_de_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_fr_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_it_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_es_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_nl_trade_partners.py
        env:
          COMTRADE_API_KEY: ${{ secrets.COMTRADE_API_KEY }}
      - run: python fetch_boe_mpc.py
      - run: python fetch_uk_inactivity_reasons.py
      - run: python fetch_uk_spending_cofog.py
      - run: python fetch_uk_age_breakdown.py
      - name: Commit updated data
        run: |
          git config user.name "data-bot"
          git config user.email "actions@github.com"
          git add data.json data-us.json data-ez.json data-jp.json data-in.json data-ca.json data-au.json data-kr.json data-il.json data-mx.json data-br.json data-za.json data-ma.json data-de.json data-fr.json data-it.json data-es.json data-nl.json
          git add data-uk-trade-partners.json 2>/dev/null || true
          git add data-us-trade-partners.json 2>/dev/null || true
          git add data-jp-trade-partners.json 2>/dev/null || true
          git add data-in-trade-partners.json 2>/dev/null || true
          git add data-ca-trade-partners.json 2>/dev/null || true
          git add data-au-trade-partners.json 2>/dev/null || true
          git add data-kr-trade-partners.json 2>/dev/null || true
          git add data-il-trade-partners.json 2>/dev/null || true
          git add data-mx-trade-partners.json 2>/dev/null || true
          git add data-br-trade-partners.json 2>/dev/null || true
          git add data-za-trade-partners.json 2>/dev/null || true
          git add data-ma-trade-partners.json 2>/dev/null || true
          git add data-de-trade-partners.json 2>/dev/null || true
          git add data-fr-trade-partners.json 2>/dev/null || true
          git add data-it-trade-partners.json 2>/dev/null || true
          git add data-es-trade-partners.json 2>/dev/null || true
          git add data-nl-trade-partners.json 2>/dev/null || true
          git add data-uk-mpc-votes.json 2>/dev/null || true
          git add data-uk-inactivity-reasons.json 2>/dev/null || true
          git add data-uk-spending-cofog.json 2>/dev/null || true
          git diff --cached --quiet || git commit -m "Refresh data"
          git push
      - name: Check data freshness
        run: python check_data_freshness.py
      - name: Report freshness status via GitHub Issue
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const result = JSON.parse(fs.readFileSync('freshness_alerts.json', 'utf8'));
            const problems = result.alerts.length + result.missing_or_unreadable_files.length;
            const label = 'data-freshness';

            const { data: existing } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: label,
            });
            const openIssue = existing[0];

            if (problems === 0) {
              if (openIssue) {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: openIssue.number,
                  body: 'All series are back within normal freshness range as of this run.',
                });
                await github.rest.issues.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: openIssue.number,
                  state: 'closed',
                });
              }
              return;
            }

            const lines = [
              `Checked at ${result.checked_at}. ${problems} issue(s) found.`, '',
            ];
            if (result.alerts.length) {
              lines.push('### Genuinely stale series (3x past normal update cadence)');
              lines.push('| Country | Series | Last published | Days overdue |');
              lines.push('|---|---|---|---|');
              for (const a of result.alerts) {
                lines.push(`| ${a.country} | ${a.label} | ${a.last_period} | ${a.age_days - a.normal_threshold_days} |`);
              }
              lines.push('');
            }
            if (result.missing_or_unreadable_files.length) {
              lines.push('### Missing or unreadable data files');
              for (const m of result.missing_or_unreadable_files) {
                lines.push(`- ${m.country} (${m.file})${m.error ? ': ' + m.error : ''}`);
              }
            }
            const body = lines.join('\n');

            if (openIssue) {
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: openIssue.number,
                body,
              });
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: '🔴 Data freshness alert',
                body,
                labels: [label],
              });
            }
